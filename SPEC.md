# ShopBlock 商店模块 · 数据格式规范（SPEC）

> 状态：格式规范定稿，待实施。本文档定义第四联动模块「商店 `<Shop_block>`」的全部数据契约。
> 格式惯例对齐三件套：正文块 = `<Combat_block>` 式中文键 YAML（RpgCombat）；独立 LLM = 副导演式英文键 JSON；跨模块 = `$` 前缀聊天变量 + 事件回执 + 数值直连不走 LLM。
> 联动对象：MiniMapStatus（mms 状态栏）、AssistantDirector（副导演：报纸/风声数据源）、RpgCombat（块解析惯例来源）。

---

## 一、正文 AI 原始 YAML：`<Shop_block>`

标签 `<Shop_block>...</Shop_block>`（解析大小写不敏感），内部纯 YAML、无 markdown 围栏。解析沿用 RpgCombat 模式：`lastIndexOf` 取最后一个块 + 内联 js-yaml 4.1.0。

```yaml
<Shop_block>
商店数据:
  店名: "出征杂货铺"
  店主: "老萨姆"
  立绘: "占卜商妇"
  店主性格: "世故圆滑的流浪商妇，见钱眼开但童叟无欺"
  在场成员:
    - "埃利奥特"
    - "弗兰克"
  分类商品:
    补给:
      - 商品:
          名字: "基廷氏杀虫粉"
          价格: "2.50美元"
          数量: 12
          单位: "罐"
          标签: "[消耗品][医用物资]"
          介绍: "教会特供的强效杀虫粉，撒一圈就能让墙缝里的东西安静一整夜。"
    武器:
      - 商品:
          名字: "S&W .38 特警左轮"
          价格: "25.00美元"
          标签: "[探员特供][双动转轮]"
          介绍: "便衣探员的标配。转轮从不卡壳，除非你的食指在发抖。"
  独家私藏:
    - 商品:
        名字: "浸血羊皮纸古籍残页"
        价格: "130.00美元"
        标签: "[极度危险][残卷]"
        介绍: "（瞳孔瞬间放大）……快拿开！听见墙壁隔层里的指甲刮擦声了吗？！"
</Shop_block>
```

### 字段规范

| 字段 | 必填 | 说明 |
|---|---|---|
| 店名 | ✓ | `$shops` 持久化主键 |
| 店主 | ✓ | 独立 LLM 对话的说话人名 |
| 立绘 | ✓ | 10 选 1 枚举（见第二节），非自由文本 |
| 店主性格 | 可选 | 一句话人设，拼入独立 LLM 的 system prompt |
| 在场成员 | ✓ | 本次进店的团队成员名单（叙事真值，正文 AI 决定谁跟着进店） |
| 分类商品 | ✓ | 自由中文类目名 → `- 商品:` 嵌套字段列表 |
| 独家私藏 | 可选 | 固定隐藏类目，结构同商品，正文 AI 每次输出 |

商品子字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| 名字 | ✓ | 商品名 |
| 价格 | ✓ | 带货币后缀字符串（"2.50美元"），程序剥离后缀参与计算 |
| 数量 | 可选 | 整数，默认 1；>1 走拉条复数购买 |
| 单位 | 可选 | 发/罐/卷/瓶 |
| 标签 | 可选 | `[a][b]` 徽章串 |
| 介绍 | ✓ | 店主口吻，悬浮/点击气泡文案（对应现有 demo 的 quote） |

### 生成约束（源头消除立绘冲突）

正文 AI 生成前，提示词注入附 10 类立绘菜单及人设锚点，要求**先选立绘类型，再按该类型的人设锚点写店主和性格**。解析端三重校验：立绘值不在枚举内 → 按店主性格关键词兜底映射 → 默认「占卜商妇」。

### 合并规则

- 二次进店同店名块合并入 `$shops`：同名商品以最新 价格/介绍/标签 覆盖、数量刷新；新商品追加进所属类目；旧类目保留。
- 同义/重复类目由「重排商品」按钮解决（见第三节）。
- 发给任何 LLM 前，正文上下文剥掉 `<Shop_block>`（同 `<Combat_block>` 剥离惯例）。
- 独家私藏在好感度 <100 时 UI 整类隐藏；独立 LLM 的 add 命令**禁止**写入此类目（私藏归正文 AI 管）。

---

## 二、立绘枚举（10 类）与资源路径（本地服务器）

立绘资源不在插件目录内，统一挂本地服务器：文件实际位于 `D:\Project\my_assets\shop_resources\`（10 张已确认齐全）。前端引用格式为 `<服务器根URL>/<文件名>`。

- **服务器根 URL 为可配置项**：进设置弹窗，随 llmConfig 持久化；跨设备/换端口只改一处。
- onerror 回退「占卜商妇」（shop.png）。
- 本地 demo 调试时根 URL 可指向 `D:\Project\my_assets\shop_resources\` 的任一静态服务地址。

| 枚举名 | 文件 | 人设锚点（写进正文 AI 提示词） |
|---|---|---|
| 铁匠机械师 | Blacksmith_Machinist.png | 老年男铁匠，粗犷寡言实干，卖刀具五金修械 |
| 黑市贩子 | black_market.png | 老年男黑市掮客，阴险谄媚，卖禁酒赃物军火 |
| 古籍书商 | book.png | 老年男古籍书商，博学神经质，卖旧书地图情报 |
| 酒馆老板 | food.png | 中年壮汉酒馆老板，粗鲁热心，卖酒食探消息 |
| 荒野猎人 | hunter.png | 青年男荒野猎人，孤僻冷峻，卖皮毛猎具向导 |
| 奢品商人 | luxury.png | 中年男奢品商人，浮夸精明，卖珠宝古董拍卖 |
| 秘石商人 | magic.png | 老年男秘石商人，狡黠神秘，卖水晶护符异物 |
| 药剂修女 | medical.png | 中年修女药剂师，冷漠严厉，卖成药针剂偏方 |
| 布道神父 | religion.png | 老年男布道神父，阴郁虔诚，卖圣物经书蜡烛 |
| 占卜商妇 | shop.png | 青年流浪女商贩，世故圆滑，卖杂货占卜兑钱 |

锚点设计：按年龄（老年/中年/青年）、性别（2 女性：修女、商妇）、道德轴（守序—中立—灰黑）、货品十类（武器/禁品/知识/消息/野外/奢侈/神秘/医疗/宗教/日用）拉开区分度，无重叠。

---

## 三、独立 LLM 返回 JSON

英文键、中文值、只输出 JSON 本体、禁止围栏与解释文字。解析沿用副导演 `extractJson` 模式：剥 ```json 围栏 → 括号字符串感知配对切片 → `JSON.parse`，失败重试 ≤2 次后回退内置关键词文案库。

三场景（进店反应 / 购买回应 / 自由对话）共用 schema：

```json
{
  "dialogue": [
    {"speaker": "老萨姆", "text": "哟，稀客。今天的水晶刚到的货，看看？"},
    {"speaker": "弗兰克", "text": "老板，上次那石头是不是有辐射？我胳膊到现在还发麻。"}
  ],
  "affection": 2,
  "commands": [
    {"cmd": "add", "category": "补给", "name": "石炭酸皂", "price": "0.80美元", "count": 20, "unit": "块", "tag": "[医用物资]", "desc": "石炭酸浸透的粗皂，洗手也能洗伤口。"}
  ]
}
```

### 代言白名单（硬规则）

`dialogue` 只能包含 **店主 + 在场成员中的 MMS 固定角色（`$mms_roster.characters`）− 当前发言的玩家角色**。玩家操控角色的台词只能来自输入框，**AI 永远不能代言**。实现双保险：

1. user prompt 给出明确白名单并声明名单外禁言；
2. 解析端校验 speaker，白名单外的行直接丢弃。

其他约束：speaker 未知名回退店主气泡；text ≤60 字；1~4 条有序（店主先说、队友可插话）。

### 字段说明

- `affection`：可选整数 -5~+5，程序累加后钳制 0~100，缺省 0。
- `commands`：仅 `add`，每次 ≤2 条，只在玩家问到店里没有的东西时于自然对话末尾附带。程序按名字去重（已存在则更新而非重复添加）。add 禁止写入 `独家私藏`。

### 重排命令（独立按钮触发，单独一次 LLM 调用）

```json
{"regroup": {"武器": ["柯尔特 M1911", "S&W .38 左轮"], "弹药": [".45 手枪弹", "12号鹿弹"]}}
```

输入为当前全部分类商品（类目→商品名清单），LLM 合并同义/重复类目；程序按名字搬家，未出现在输出中的商品原地不动，独家私藏不参与，结果弹确认框可撤销。

### 请求组装（三场景共用上下文包）

- **system**：店主名 + 店主性格 + 立绘锚点基调 + 当前好感度 + 输出格式约束 + 代言白名单。
- **user**：最近 2 层正文上下文（`getChatMessages`，剥各 Block 标签）+ 报纸（回溯楼层取最近 `<Newday>` 的 newspaper_name/headline/快讯标题）+ 风声（聊天变量 `$ad_world.winds` 的 content·source·spread）+ 当前商店商品清单（`jsyaml.dump($shops[店名])`）+ 在场成员名单（含头像可用标记）+ 场景触发器：
  - 进店：小队刚进店 → 店主先对玩家作出反应，队友可插话；
  - 购买：购物车明细 + 总价 + 讲价结果 → 店主回应，队友插话；
  - 对话：当前发言人 + 玩家输入文本。
- **调用通道**：沿用现有 demo 的裸 `fetch` OpenAI 兼容接口（apiBase/apiKey/model 自有配置弹窗），失败回退内置关键词文案库。

---

## 四、持久化 `$shops`（chat 变量，中文键直存，以店名为键）

读写：`getVariables / insertOrAssignVariables`，`type:'chat'`。中文键直存的意义：回访注入提示词时 `jsyaml.dump` 原样喂给正文 AI，零转换层。

```json
{
  "出征杂货铺": {
    "店名": "出征杂货铺",
    "店主": "老萨姆",
    "立绘": "占卜商妇",
    "店主性格": "世故圆滑的流浪商妇，见钱眼开但童叟无欺",
    "好感度": 35,
    "在场成员": ["埃利奥特", "弗兰克"],
    "分类商品": {
      "补给": [
        {"商品": {"名字": "基廷氏杀虫粉", "价格": "2.50美元", "数量": 12, "单位": "罐", "标签": "[消耗品][医用物资]", "介绍": "……"}}
      ]
    },
    "独家私藏": [
      {"商品": {"名字": "浸血羊皮纸古籍残页", "价格": "130.00美元", "标签": "[极度危险][残卷]", "介绍": "……"}}
    ],
    "$meta": {"version": 1, "floorId": 42, "ts": 1757900000000}
  }
}
```

- 好感度初始 30（可调），`affection` 命令累加，钳制 0~100；**≥100 解锁独家私藏类目显示与购买**。
- 回访时 `$shops[店名]` 经 `jsyaml.dump` 注入正文 AI（深度 0，副导演词条同款模式），保证店名/店主/私藏续写一致。
- 删除店铺必须 `replaceVariables` 整体替换（`insertOrAssign` 深合并删不掉键，MMS 已有教训）。

---

## 五、MMS 直连契约（数值不走 LLM）

### 读取（进店时）

- **团队在场/头像**：`getLastMessageId()` 向前回溯 ≤50 楼找 `$mms_meta.mesId` 匹配的 `stat_data`（必须校验 mesId 防 getVariables(message) 向前继承陷阱）；在场团队 = `stat_data.状态栏.角色列表`，与 Shop_block 的 `在场成员` 取交集定最终名单。头像按 MMS 三路取值：`AVATAR_MAP[纯名]` → 名册 `avatar` 字段解析 → `$mms_portrait_choice[纯名]` 覆盖，全空则首字 emoji 兜底。
- **团队现金**：`stat_data` 第一个含 `现金` 字段的固定模块（NewDay 的 readStatusbarCash 同款回溯模式）。

### 写入（结算时，玩家逐件选择归属：某角色 or 载具）

- 商店模块直接写最新 AI 楼的 `stat_data`（仿 MMS `applyCombatResult` 单写者模式）：
  - 现金按 MMS 现金命令语义扣减（保留货币后缀）；
  - 个人物品追加进 `角色列表[该角色].物品`；
  - 载具物品追加进 `载具` 独立模块的清单字段；
  - `replaceVariables` + `eventEmit('mms:status-updated')` 通知 MMS 刷新。
- 同时写真值载荷 `$shop_deal_result`（chat 变量，幂等 id）供对账：

```json
{
  "v": 1,
  "id": "shop-1697xxxx",
  "ts": 1757900000000,
  "floorId": 42,
  "店名": "出征杂货铺",
  "cashDelta": -35.00,
  "purchases": [
    {"owner": "埃利奥特", "name": "柯尔特 M1911", "count": 1, "category": "武器", "toVehicle": false},
    {"owner": "弗兰克", "name": ".45 军规手枪弹", "count": 24, "category": "弹药", "toVehicle": true}
  ],
  "bargain": {"requested": 10, "rolled": 62, "success": true, "discount": 10}
}
```

- 正文 AI 对购买/搬运过程的叙述：依赖上文注入的商店数据自然生成，无额外格式。

---

## 六、讲价投骰（后台程序，不经 LLM）

- 砍价幅度 d%（限 5~30%）。
- 判定公式：`1d100 + 好感度×0.5 ≥ 40 + d×2`（例：砍 10% 需过 60，好感 50 时均摊 +25）。
- 成功 → 购物车按折扣价一次性结算；失败 → 原价成交且好感 -1。
- 成功/失败时店主气泡显示**代码内置**对应文案库（非 LLM 生成），失败文案附好感下降提示。

---

## 实施路线（后续轮次）

1. 改造 `index.html`：`<Shop_block>` 解析、`$shops` 读写、立绘枚举与服务器 URL 配置、独立 LLM 模块（三场景 + add/regroup）、代言白名单、讲价投骰、MMS 直连桥。
2. 补构建链（build 脚本 + 正则 JSON 产物）与 `integration-test/harness.html`。
3. IAB mock 测试 → 用户导入真机验证。
