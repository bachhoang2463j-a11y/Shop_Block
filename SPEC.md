# ShopBlock 商店模块 · 数据格式规范（SPEC）

> 状态：M1-M9 格式契约已全部落地（现状见 README，施工史见 LOG）。本文档定义第四联动模块「商店 `<Shop_block>`」的全部数据契约。
> 格式惯例对齐三件套：正文块 = `<Combat_block>` 式中文键 YAML（RpgCombat）；独立 LLM = 副导演式英文键 JSON；跨模块 = `$` 前缀聊天变量 + 事件回执 + 数值直连不走 LLM。
> 联动对象：MiniMapStatus（mms 状态栏）、AssistantDirector（副导演：报纸/风声数据源）、RpgCombat（块解析惯例来源）。

---

## 一、正文 AI 原始 YAML：`<Shop_block>`

标签 `<Shop_block>...</Shop_block>`（解析大小写不敏感），内部纯 YAML、无 markdown 围栏。解析沿用 RpgCombat 模式：`lastIndexOf` 取最后一个块 + 内联 js-yaml 4.1.0。取数通道同样对齐 RpgCombat：正则只替换 iframe 空壳，块数据由组件在 iframe 内轮询楼层自取（见下「读取通道」）。

```yaml
<Shop_block>
商店数据:
  店名: "出征杂货铺"
  店主: "老萨姆"
  立绘: "占卜商妇"
  店主性格: "世故圆滑的流浪商妇，见钱眼开但童叟无欺"
  在场成员:
    - "埃利奥特"
    - "弗兰克"
  分类商品:
    补给:
      - 商品:
          名字: "基廷氏杀虫粉"
          价格: "2.50美元"
          数量: 12
          单位: "罐"
          标签: "[消耗品][医用物资]"
          介绍: "教会特供的强效杀虫粉，撒一圈就能让墙缝里的东西安静一整夜。"
    武器:
      - 商品:
          名字: "S&W .38 特警左轮"
          价格: "25.00美元"
          标签: "[探员特供][双动转轮]"
          介绍: "便衣探员的标配。转轮从不卡壳，除非你的食指在发抖。"
  独家私藏:
    - 商品:
        名字: "浸血羊皮纸古籍残页"
        价格: "130.00美元"
        标签: "[极度危险][残卷]"
        介绍: "（瞳孔瞬间放大）……快拿开！听见墙壁隔层里的指甲刮擦声了吗？！"
</Shop_block>
```

### 字段规范

| 字段 | 必填 | 说明 |
|---|---|---|
| 店名 | ✓ | `$shops` 持久化主键 |
| 店主 | ✓ | 独立 LLM 对话的说话人名 |
| 立绘 | ✓ | 10 选 1 枚举（见第二节），非自由文本 |
| 店主性格 | 可选 | 一句话人设，拼入独立 LLM 的 system prompt |
| 在场成员 | ✓ | 本次进店的团队成员名单（叙事真值，正文 AI 决定谁跟着进店） |
| 分类商品 | ✓ | 自由中文类目名 → `- 商品:` 嵌套字段列表 |
| 独家私藏 | 可选 | 固定隐藏类目，结构同商品，正文 AI 每次输出 |

商品子字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| 名字 | ✓ | 商品名，支持 `🧪 名字` 前缀即图标（解析端剥离前缀存图标、名字栏只留干净名；无前缀默认 📦） |
| 价格 | ✓ | 带货币后缀字符串（"2.50美元"），程序剥离后缀参与计算 |
| 数量 | 可选 | 整数，默认 1；>1 走拉条复数购买 |
| 单位 | 可选 | 发/罐/卷/瓶 |
| 标签 | 可选 | `[a][b]` 徽章串 |
| 介绍 | ✓ | 店主口吻，悬浮/点击气泡文案（对应现有 demo 的 quote） |

### 生成约束（源头消除立绘冲突）

正文 AI 生成前，提示词注入附 10 类立绘菜单及人设锚点，要求**先选立绘类型，再按该类型的人设锚点写店主和性格**。解析端三重校验：立绘值不在枚举内 → 按店主性格关键词兜底映射 → 默认「占卜商妇」。

### 合并规则

- 二次进店同店名块合并入 `$shops`：同名商品以最新 价格/介绍/标签 覆盖、数量刷新；新商品追加进所属类目；旧类目保留。新旧名比较按剥 emoji 前缀后的干净名（"🧪杀虫粉"视同"杀虫粉"）。
- 同义/重复类目由「重排商品」按钮解决（见第三节）。
- 发给任何 LLM 前，正文上下文剥掉 `<Shop_block>` 与 `<Shop_Record>`（同 `<Combat_block>` 剥离惯例）。
- 独家私藏在好感度 <100 时 UI 整类隐藏；独立 LLM 的 add 命令**禁止**写入此类目（私藏归正文 AI 管）。

### 读取通道（楼层自动捕获，RpgCombat `startSTPolling` 同构）

正则产物只负责把正文最后一个块整段替换成组件 iframe——`$1` 不传入组件（`$数字` 在构建期免疫，见 build-regex.cjs），**块数据由组件自己在 iframe 内取**：

1. 启动首 tick 立即扫 + 每 800ms 轮询一次：读本层原文 `getChatMessages(getCurrentMessageId())`（字段兼容 `raw_content || message || mes`，跳过 `is_system`），退化通道 `getCurrentMessage()`；
2. `extractShopBlock` 取最后一个块 → `onShopDataReceived` → 解析渲染 + `$shops` 持久化 + 进店反应；
3. 命中即停表；已应用块文本去重（同块重复 tick 不再应用，保护购物车与砍价会话态）；
4. **手动导入优先**：`onShopDataReceived` 成功后一律停轮询，粘过的块不会被随后的轮询覆盖；
5. 5s 未扫到 → 提示条「未检测到 `<Shop_block>`，当前为示例商店，可手动粘贴」；30s 硬上限停表；
6. 非酒馆环境（`getChatMessages` / `getCurrentMessageId` / `getCurrentMessage` 全缺）**不建定时器**，行为与纯手动版一致（独立 IAB / harness 零副作用）。

**已知边界**：轮询钉「本 iframe 启动时的当前楼层」，不向前回溯找更早的块——重载聊天后旧楼层的 iframe 若当前楼层无块，会停在示例商店并给提示，走手动粘贴回退。取舍依据：宁可不显示数据，也不把别的楼的店显示到这一楼。重复渲染同一楼层会重复合并 `$shops`（按店名幂等，商品不重复追加）。

---

## 二、立绘枚举（10 类）与资源路径（本地服务器）

立绘资源不在插件目录内，统一挂本地服务器：文件实际位于 `D:\Project\my_assets\shop_resources\`（10 张已确认齐全）。前端引用格式为 `<服务器根URL>/<文件名>`。

- **服务器根 URL 为可配置项**：进设置弹窗，随 llmConfig 持久化；跨设备/换端口只改一处。
- onerror 回退「占卜商妇」（shop.png）。
- 本地 demo 调试时根 URL 可指向 `D:\Project\my_assets\shop_resources\` 的任一静态服务地址。

| 枚举名 | 文件 | 人设锚点（写进正文 AI 提示词） |
|---|---|---|
| 铁匠机械师 | Blacksmith_Machinist.png | 老年男铁匠，粗犷寡言实干，卖刀具五金修械 |
| 黑市贩子 | black_market.png | 老年男黑市掮客，阴险谄媚，卖禁酒赃物军火 |
| 古籍书商 | book.png | 老年男古籍书商，博学神经质，卖旧书地图情报 |
| 酒馆老板 | food.png | 中年壮汉酒馆老板，粗鲁热心，卖酒食探消息 |
| 荒野猎人 | hunter.png | 青年男荒野猎人，孤僻冷峻，卖皮毛猎具向导 |
| 奢品商人 | luxury.png | 中年男奢品商人，浮夸精明，卖珠宝古董拍卖 |
| 秘石商人 | magic.png | 老年男秘石商人，狡黠神秘，卖水晶护符异物 |
| 药剂修女 | medical.png | 中年修女药剂师，冷漠严厉，卖成药针剂偏方 |
| 布道神父 | religion.png | 老年男布道神父，阴郁虔诚，卖圣物经书蜡烛 |
| 占卜商妇 | shop.png | 青年流浪女商贩，世故圆滑，卖杂货占卜兑钱 |

锚点设计：按年龄（老年/中年/青年）、性别（2 女性：修女、商妇）、道德轴（守序—中立—灰黑）、货品十类（武器/禁品/知识/消息/野外/奢侈/神秘/医疗/宗教/日用）拉开区分度，无重叠。

---

## 三、独立 LLM 返回 JSON

英文键、中文值、只输出 JSON 本体、禁止围栏与解释文字。解析沿用副导演 `extractJson` 模式：剥 ```json 围栏 → 括号字符串感知配对切片 → `JSON.parse`，失败重试 ≤2 次后回退内置关键词文案库。

三场景（进店反应 / 购买回应 / 自由对话 / 砍价回应，共四种触发器）共用 schema：

```json
{
  "dialogue": [
    {"speaker": "老萨姆", "text": "哟，稀客。今天的水晶刚到的货，看看？"},
    {"speaker": "弗兰克", "text": "老板，上次那石头是不是有辐射？我胳膊到现在还发麻。"}
  ],
  "affection": 2,
  "commands": [
    {"cmd": "add", "category": "补给", "name": "🧼 石炭酸皂", "price": "0.80美元", "count": 20, "unit": "块", "tag": "[医用物资]", "desc": "石炭酸浸透的粗皂，洗手也能洗伤口。"}
  ]
}
```

### 代言白名单（硬规则）

`dialogue` 只能包含 **店主 + 在场成员中的 MMS 固定角色（`$mms_roster.characters`）− 当前发言的玩家角色**。玩家操控角色的台词只能来自输入框，**AI 永远不能代言**。实现双保险：

1. user prompt 给出明确白名单并声明名单外禁言；
2. 解析端校验 speaker，白名单外的行直接丢弃。

其他约束：speaker 未知名回退店主气泡；text ≤60 字；1~4 条有序（店主先说、队友可插话）。

### 字段说明

- `dialogue`：1~4 条有序（店主先说、队友可插话）。speaker 必须是店主名或在场队友名，未知名字回退到店主气泡；text ≤60 字。
- `affection`：**凡含玩家台词的场景（自由对话 / 砍价 / 购买回应）必返**，整数 -5~+5，表示本次玩家对话让店主产生的态度变化（礼貌 / 冒犯 / 投机 / 共情 / 爽快程度），程序累加后钳制 0~100。进店反应（无玩家台词）不返回。注意砍价场景的好感总变动 = 本字段（LLM 判玩家言辞）+ 程序贪婪惩罚（第六节），两者叠加。
- `commands`：仅 `add`，只在玩家问到店里没有的东西时于自然对话末尾附带。add 禁止写入 `独家私藏`。`name` 须带 emoji 前缀（"🧪 名字"，无合适可省略，前端回落 📦）。

**add 命令四重限制（反许愿机）**：

1. **渠道合理性（LLM 侧）**：只有常规补给类商品（食品、日用、常规药品、通用弹药等普货）可 add；违禁品、神话物品、超时代科技、大宗军火必须拒绝，店主以人设口吻回一句"弄不到"的说辞。提示词明确"你不是许愿机，货必须有来路"。
2. **类型锚点校验（程序侧）**：add 商品的 category 必须落在该店立绘类型的货品锚点范围内（第二节枚举表的货品倾向），越界命令直接丢弃（对话照常显示，货不上架）。
3. **频率限制（程序侧）**：每次进店会话 add 落地 ≤1 条，超出的静默丢弃；程序按名字去重（已存在则更新而非重复添加）。
4. **价格约束（LLM 侧）**：add 商品价格参照店内同类商品定价，不得免费或象征性收费；讲价投骰对 add 商品同样适用。

### 重排命令（独立按钮触发，单独一次 LLM 调用）

```json
{"regroup": {"武器": ["柯尔特 M1911", "S&W .38 左轮"], "弹药": [".45 手枪弹", "12号鹿弹"]}}
```

输入为当前全部分类商品（类目→商品名清单），LLM 合并同义/重复类目；程序按名字搬家，未出现在输出中的商品原地不动，独家私藏不参与，结果弹确认框可撤销。

### 请求组装（三场景共用上下文包）

- **system**：店主名 + 店主性格 + 立绘锚点基调 + 当前好感度 + 输出格式约束 + 代言白名单。
- **user**：最近 2 层正文上下文（`getChatMessages`，剥各 Block 标签）+ 报纸（回溯楼层取最近 `<Newday>` 的 newspaper_name/headline/快讯标题）+ 风声（聊天变量 `$ad_world.winds` 的 content·source·spread）+ 当前商店商品清单（`jsyaml.dump($shops[店名])`）+ 在场成员名单（含头像可用标记）+ 场景触发器：
  - 进店：小队刚进店 → 店主先对玩家作出反应，队友可插话；
  - 购买：购物车明细 + 总价 + 讲价结果 → 店主回应，队友插话；
  - 对话：当前发言人 + 玩家输入文本；
  - 砍价：玩家斜体砍价宣言（含补写的台词）+ 投骰结果分支 → 店主以人设口吻回应（见第六节四分支）。

**可选数据源降级原则（硬规则）**：商店与 MMS 强耦合（数值直连），但与副导演**弱耦合**——副导演未启用时，`$ad_world`（风声）、`<Newday>`（报纸）等一切可选数据源必须**自动降级为空**（对应 prompt 段落整体省略），绝不抛错、绝不阻塞、绝不影响商店可用性。实现统一模式：`typeof API === 'function'` 守卫 + try/catch 返回空值，与 `collectRecentStoryFloors` 的楼层守卫同款。
- **调用通道**：沿用现有 demo 的裸 `fetch` OpenAI 兼容接口（apiBase/apiKey/model 自有配置弹窗），失败回退内置关键词文案库。

---

## 四、持久化 `$shops`（chat 变量，中文键直存，以店名为键）

读写：`getVariables / insertOrAssignVariables`，`type:'chat'`。中文键直存的意义：回访注入提示词时 `jsyaml.dump` 原样喂给正文 AI，零转换层。

```json
{
  "出征杂货铺": {
    "店名": "出征杂货铺",
    "店主": "老萨姆",
    "立绘": "占卜商妇",
    "店主性格": "世故圆滑的流浪商妇，见钱眼开但童叟无欺",
    "好感度": 35,
    "在场成员": ["埃利奥特", "弗兰克"],
    "分类商品": {
      "补给": [
        {"商品": {"名字": "基廷氏杀虫粉", "价格": "2.50美元", "数量": 12, "单位": "罐", "标签": "[消耗品][医用物资]", "介绍": "……"}}
      ]
    },
    "独家私藏": [
      {"商品": {"名字": "浸血羊皮纸古籍残页", "价格": "130.00美元", "标签": "[极度危险][残卷]", "介绍": "……"}}
    ],
    "$meta": {"version": 1, "floorId": 42, "ts": 1757900000000}
  }
}
```

- 好感度初始 30（可调），按第六节三来源算法累加（对话/购物/砍价惩罚），钳制 0~100；**≥100 解锁独家私藏类目显示与购买**。
- 回访时 `$shops[店名]` 经 `jsyaml.dump` 注入正文 AI（深度 0，副导演词条同款模式），保证店名/店主/私藏续写一致。
- 删除店铺必须 `replaceVariables` 整体替换（`insertOrAssign` 深合并删不掉键，MMS 已有教训）。

---

## 五、MMS 直连契约（数值不走 LLM）

### 读取（进店时）

- **团队在场/头像**：`getLastMessageId()` 向前回溯 ≤50 楼找 `$mms_meta.mesId` 匹配的 `stat_data`（必须校验 mesId 防 getVariables(message) 向前继承陷阱）；在场团队 = `stat_data.状态栏.角色列表`，与 Shop_block 的 `在场成员` 取交集定最终名单。头像按 MMS 三路取值：`AVATAR_MAP[纯名]` → 名册 `avatar` 字段解析 → `$mms_portrait_choice[纯名]` 覆盖，全空则首字 emoji 兜底。
- **团队现金**：`stat_data` 第一个含 `现金` 字段的固定模块（NewDay 的 readStatusbarCash 同款回溯模式）；读到即回显到顶栏现钞（`playerState.cash`），结算写回成功后同步刷新，保证店内金额与状态栏一致。
- **降级语义（不得静默）**：MMS 数据不可读（无 API / 本层及前 50 楼无 `stat_data`）→ 底栏保持示例 4 人、现钞保持示例金额，**提示条常驻**说明「未检测到 MMS 状态栏数据，结算不会写入状态栏」；块 `在场成员` 与名册**零匹配** → 底栏置空 + 占位说明 + 提示条常驻（发言与砍价入口随之关闭并给出原因），不再静默回退显示全部名册。两种状态由 `mmsDataState`（`ok`/`unavailable`/`mismatch`）标记，手动导入成功时同步回显该状态。

### 写入（结算时，玩家逐件选择归属：某角色 or 载具）

- 商店模块直接写最新 AI 楼的 `stat_data`（仿 MMS `applyCombatResult` 单写者模式）：
  - 现金按 MMS 现金命令语义扣减（保留货币后缀）；
  - 个人物品追加进 `角色列表[该角色].物品`；
  - 载具物品追加进 `载具` 独立模块的清单字段；
  - `replaceVariables` + `eventEmit('mms:status-updated')` 通知 MMS 刷新。
- 同时写真值载荷 `$shop_deal_result`（chat 变量，幂等 id）供对账：

```json
{
  "v": 1,
  "id": "shop-1697xxxx",
  "ts": 1757900000000,
  "floorId": 42,
  "店名": "出征杂货铺",
  "cashDelta": -35.00,
  "purchases": [
    {"owner": "埃利奥特", "name": "柯尔特 M1911", "count": 1, "category": "武器", "toVehicle": false},
    {"owner": "弗兰克", "name": ".45 军规手枪弹", "count": 24, "category": "弹药", "toVehicle": true}
  ],
  "bargain": {"requested": 10, "rolled": 62, "success": true, "discount": 10},
  "lock": {
    "cash": true,
    "chars": [{"name": "埃利奥特", "items": ["柯尔特 M1911"]}],
    "vehicles": [{"name": "防弹轿车", "items": [".45 军规手枪弹"]}]
  }
}
```

### 二次扣除锁（与 RpgCombat 战斗硬锁 `$mms_combat_sync` 同款）

商店结算的数值已由程序直写 `stat_data`，但后续楼层的 **MMS 独立更新 LLM** 读到购买叙事（`<Shop_Record>` 续写出的正文）时可能再次扣现金/加物品。防御分三层：

1. **幂等台账 `$shop_sync`**（chat 变量，商店侧）：`{lastId, appliedFloor, ts}`。结算写 `stat_data` 前比对 `lastId === 本次结算 id` 则跳过——防重试/事件重放/重复点击导致的程序侧重复写入（战斗侧 11762 行同款语义）。
2. **合并层硬锁（MMS 侧契约，MiniMapStatus 后续任务）**：`$shop_deal_result.lock` 是硬锁清单——`cash:true` 锁现金字段，`chars[].items`/`vehicles[].items` 锁对应角色/载具的物品条目。MMS 更新合并时（`mmsMergeState` 的 combatLock 通道），锁定项的 AI 数值命令**一律丢弃**（10435 行语义：'丢弃更新 AI 对已由程序写入的属性键/物品的数值命令'），AI 的自由字段输出不受影响。
3. **锁窗口**：硬锁只在购买叙事仍处于更新 LLM 上下文窗口内时有效（战斗侧 11733 行同款），叙事滚出窗口后锁过期，避免永久锁死 LLM 对这些字段的正常维护。

纵深防御：MMS 更新提示词追加"商店结算禁令"（战斗结算禁令 13724 行同款位置）——但真正的拦截在合并层硬锁，提示词只是第一道。

- 正文 AI 对购买/搬运过程的叙述：依赖上文注入的商店数据自然生成，无额外格式。

---

## 六、讲价与好感度算法

### 讲价（砍价按钮 + 后台投骰 + LLM 分支回应）

- **交互**：输入框右侧「砍价」按钮 → 单击目标商品 → 砍价拉条（幅度 5~30%）→ 确定 → 程序把斜体包裹的 `*{{角色名}}尝试砍价，砍价后价格为XX。*` 自动填入对话框，玩家可续写角色台词或直接发送。
- **投骰**（发送时执行，不经 LLM）：`1d100 + 好感度×0.5 ≥ 40 + 幅度×2`。
- **LLM 四分支提示词**（按投骰/校验结果选一支，店主以人设口吻回应）：
  1. 成功 → 爽快或勉强答应（按人设）；
  2. 首次失败 → 拒绝；
  3. 二次失败 → 明显不耐烦，态度按人设演绎；
  4. 二次更大幅度 → 免投骰直接拒绝，提示词注明"玩家得寸进尺"。
  LLM 不可用或解析失败时回退内置文案库（四分支各备文案）。
- **次数限制**：同一商品每次进店会话最多砍两次；第二次幅度必须**更小**才重新投掷，更大直接走分支 4。
- **结果落地**：成功 → 该商品按折后价入车，**且程序自动把折后价写回商品柜**——商品卡显示划线原价 + 折后价 + 砍价者角标，本店会话内任何人购买均按折后价；限砍计数与锁定价为**会话内存态**（不写入 `$shops`，刷新/重进店重新解析块后重置为块内原价）；失败维持原价，玩家可放弃或原价购买。

### 好感度算法（程序结算，钳制 0~100，三来源叠加）

| 来源 | 触发时机 | 数值 | 判定方 |
|---|---|---|---|
| 对话好感 | 每次含玩家台词的场景（自由对话 / 砍价 / 购买回应） | LLM 返回 `affection` 字段 -5~+5，按玩家言辞判定 | 独立 LLM |
| 购物好感 | 每次结算成功（折后实付金额） | 每满 5 美元 +1，不设单次上限（1920 年代 5 美元已是小巨款） | 程序 |
| 砍价惩罚 | 砍价失败 / 二次贪婪 | 首次失败 `-⌈幅度÷10⌉`（-1~-3）；二次失败翻倍 `-2×⌈幅度÷10⌉`；二次更大幅度被直接拒绝 -2；成功不扣 | 程序 |

- 初始值 30；好感度影响三处：100 解锁独家私藏、system prompt 中的店主态度基调、投骰公式加值。
- 砍价场景总变动 = LLM 对话判定 + 程序贪婪惩罚（嘴甜可小幅挽回，出言不逊雪上加霜）。

---

## 七、商店记录模块 `<Shop_Record>`（注入聊天框续写）

结算完成后，程序把本次进店会话组装成记录文本，**填入酒馆输入框**，玩家检查/修改后发送，正文 AI 据此续写购买与搬运过程。会话对话流水在进店期间内存收集，不持久化。

```html
<Shop_Record>
商店: 出征杂货铺 (店主: 老萨姆 · 立绘: 占卜商妇 · 讲价结果: 10%成功)
本次对话:
  埃利奥特: "老板，上次那批杀虫粉还有吗？"
  老萨姆: "哟，稀客。这周刚到的货，就是涨了点价。"
  弗兰克: "涨价？看在老主顾份上便宜点。"
  *埃利奥特尝试砍价，砍价后价格为XX。*
  老萨姆: "行吧，看你是熟面孔。"
购买清单:
  - 基廷氏杀虫粉 ×6 = $15.00（折后价）→ 归属: 埃利奥特
  - 石炭酸皂 ×4 = $3.20（原价）→ 归属: 载具（防弹轿车）
实付总额: $18.20，团队现金余额: $4744.70
</Shop_Record>
```

**内容四要素**：玩家发送的全部消息（含砍价斜体宣言）、所有 LLM 返回的对话（店主+队友）、最终购买清单（数量/价格/折后）、物品归属（角色 or 载具）。

**规则**：
- 玩家发送前可删改记录文本（不想让正文 AI 看到的插话可抹掉）。
- `<Shop_Record>` 加入上下文剥离列表：商店独立 LLM 组装上下文时与 `<Shop_block>` 一并剥掉，避免记录复读。
- 与 `$shop_deal_result` 分工：前者是给正文 AI 的叙事载体，后者是给 MMS/程序对账的数值真值。

---

## 实施路线（后续轮次）

1. 改造 `index.html`：`<Shop_block>` 解析、`$shops` 读写、立绘枚举与服务器 URL 配置、独立 LLM 模块（三场景 + add/regroup）、代言白名单、讲价投骰、MMS 直连桥。
2. 补构建链（build 脚本 + 正则 JSON 产物）与 `integration-test/harness.html`。
3. IAB mock 测试 → 用户导入真机验证。
